# Frontend Environment Variables
# Copy this file to .env.local and fill in your values

# Backend URL (your Render server URL)
VITE_BACKEND_URL=https://your-app-name.onrender.com

# Note: The frontend no longer needs VITE_API_KEY since all AI calls go through the backend
# The backend uses GEMINI_API_KEY_BACKEND environment variable
